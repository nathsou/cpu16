
module ResetConditioner<Cycles: uint>(
    clk: clock,
    d: bool,
) -> (
    q: bool,
) {
    let counter = Reg<uint<$clog2(Cycles - 1)>>(clk)

    q = if counter.q != Cycles - 1 {
        counter.d = counter.q + 1
        true
    } else {
        d
    }
}
